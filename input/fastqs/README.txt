Please copy your input FASTQ files into this directory. Make sure you have named your files with "R1" and "R2" (and optional "R3") instead of Illumina outputs "R1", "I1", (optional "I2").

Then update the config.yaml file input_dir variable as follows: 
input_dir: input/fastqs/
